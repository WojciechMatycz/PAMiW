Przebieg zajêæ
Przybli¿ony przebieg zajêæ:

Zapisy studentów na zajêcia.
Przedstawienie regulaminu laboratorium.
Przedstawienie przebiegu dalszego przebiegu zajêæ, wymagañ i warunków zaliczenia.
Wyjaœnienie wszelkich w¹tpliwoœci formalnych.
Przygotowanie œrodowiska informatycznego do prowadzenia zajêæ.
Wprowadzenie do pracy z sytemem UNIX w trybie linii poleceñ.
Wprowadzenie do programowania w Shellu.
Wprowadzenie do programowania w Pythonie.
 

Æwiczenia
pliki startowe pow³oki
wbudowane mechanizmy nawigacji. historia
zmienne œrodowiska
praca ze strumieniami
Trywialny przyk³ad skryptu w shellu:

if [ "$LANG" = "pl_PL" ] 
  then
    echo Czesc
  else
    echo Hello
fi
Zapiszmy systemow¹ bazê u¿ytkowników do pliku:

$ getent passwd > passwd
Przyk³ad 1

import posix
import string
uid = posix.getuid()
login = ''
passwd = open('passwd')
for line in passwd.readlines():
        rec = string.split(line, ':')
        if int(rec[2]) == uid:
                login = rec[0]
                break
if login:
        print "Witaj, %s" % login
else:
        print "Nie ma Cie w passwd"
Przyk³ad 2

import sys
import string
login = sys.argv[1]
username = {}
passwd = open('passwd')
for line in passwd.readlines():
        rec = string.split(line, ':')
        username[rec[0]] = rec[4]
print "%s to %s" % (login, username[login])
Przyk³ad 3

import sys
import string
text = sys.argv[1]
counter = 0
for line in sys.stdin.readlines():
        if string.count(line, text):
                print line
                counter += 1    
print "Znaleziono %d linii zawierajacych %s" % (counter, text)
 

Ocenianie
Na tych zajêciach (wyj¹tkowo) nie jest przewidywane wystawianie ocen.

 

Materia³y dodatkowe
W celu poæwiczenia pracy z systemem UNIX polecamy zainstalowanie komputerze jakiejkolwiek dystrybucji Linuksa, albo FreeBSD. Mo¿na te¿ zdalnie pracowaæ na jednym z serwerów uczelnianych.

Programowanie w shellu jest bardzo dobrze udokumentowane w samym systemie, wystarczy napisaæman bash.

Podrêcznik do pythona otrzymamy za pomoc¹ polecenia man python

Has³a dla Google: shell programming, introduction to python, wprowadzenie do pythona